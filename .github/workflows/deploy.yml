name: Legacy Webigram Deploy (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This legacy workflow is intentionally disabled."
          echo "Use deploy-fast.yml for production deployments."
