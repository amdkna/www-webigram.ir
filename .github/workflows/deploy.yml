name: Deploy Webigram to Production Server

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: webigram-production-v2
  # Never kill a production deployment halfway through a container/file swap.
  # A newer deployment waits for the current one to finish (or roll back).
  cancel-in-progress: false

jobs:
  build-site:
    name: Build static site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build site image
        run: docker build --tag webigram-build:${GITHUB_SHA} .

      - name: Extract dist
        run: |
          set -e
          rm -rf /tmp/webigram-dist
          mkdir -p /tmp/webigram-dist
          CID=$(docker create webigram-build:${GITHUB_SHA})
          docker cp "$CID:/usr/share/nginx/html/." /tmp/webigram-dist/
          docker rm "$CID"

          test -f /tmp/webigram-dist/index.html
          test -s /tmp/webigram-dist/test6/index.html
          test -s /tmp/webigram-dist/home6/index.html
          test -s /tmp/webigram-dist/services/index.html
          test -s /tmp/webigram-dist/portfolio/index.html
          test -s /tmp/webigram-dist/contact/index.html
          test -s /tmp/webigram-dist/blog/index.html
          test -f /tmp/webigram-dist/tools/website-doctor/index.html
          test -f /tmp/webigram-dist/downloads/webigram-website-doctor-chrome.zip

      - name: Publish production build
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -e
          cd /tmp/webigram-dist
          git init -b production-build
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .
          git commit -m "Build ${GITHUB_SHA}"
          git push --force "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:production-build

  deploy:
    name: Deploy on server
    needs: build-site
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 12
    env:
      APP_DIR: /opt/apps/www-webigram.ir
      PUBLIC_HEALTH_URL: https://webigram.ir/healthz
      EDGE_CONTAINER: edge-nginx
    steps:
      - name: Snapshot last working production release
        run: |
          set -euo pipefail
          mkdir -p "$APP_DIR"

          if [ ! -d "$APP_DIR/.git" ]; then
            git clone https://github.com/amdkna/www-webigram.ir.git "$APP_DIR"
          fi

          ROLLBACK_DIR="$APP_DIR/.deploy-rollback"
          rm -rf "$ROLLBACK_DIR"
          mkdir -p "$ROLLBACK_DIR"

          if git -C "$APP_DIR" rev-parse --verify HEAD >/dev/null 2>&1 && [ -f "$APP_DIR/dist/index.html" ]; then
            git -C "$APP_DIR" rev-parse HEAD > "$ROLLBACK_DIR/main.sha"
            cp -a "$APP_DIR/dist" "$ROLLBACK_DIR/dist"
            touch "$ROLLBACK_DIR/available"
            echo "Rollback snapshot created for $(cat "$ROLLBACK_DIR/main.sha")"
          else
            echo "No previous complete release found; this appears to be the first deployment."
          fi

      - name: Prepare new release files
        run: |
          set -euo pipefail
          git -C "$APP_DIR" fetch origin main production-build
          git -C "$APP_DIR" checkout -B main origin/main

          rm -rf "$APP_DIR/dist.new"
          mkdir "$APP_DIR/dist.new"
          git -C "$APP_DIR" archive origin/production-build | tar -x -C "$APP_DIR/dist.new"

          # Do not touch live files unless the candidate build is complete.
          test -s "$APP_DIR/dist.new/index.html"
          test -s "$APP_DIR/dist.new/test6/index.html"
          test -s "$APP_DIR/dist.new/home6/index.html"
          test -s "$APP_DIR/dist.new/services/index.html"
          test -s "$APP_DIR/dist.new/portfolio/index.html"
          test -s "$APP_DIR/dist.new/contact/index.html"
          test -s "$APP_DIR/dist.new/blog/index.html"
          test -s "$APP_DIR/dist.new/tools/website-doctor/index.html"
          test -s "$APP_DIR/dist.new/downloads/webigram-website-doctor-chrome.zip"

          # Preserve the live dist directory inode because it is bind-mounted into nginx.
          mkdir -p "$APP_DIR/dist"
          cp -a "$APP_DIR/dist.new/." "$APP_DIR/dist/"
          rm -rf "$APP_DIR/dist.new"

      - name: Prepare docker
        run: |
          set -euo pipefail
          docker image inspect nginx:1.28-alpine >/dev/null 2>&1 || docker pull nginx:1.28-alpine
          docker image inspect node:24-alpine >/dev/null 2>&1 || docker pull node:24-alpine
          docker network inspect public_proxy >/dev/null 2>&1 || docker network create public_proxy
          docker network inspect app-net >/dev/null 2>&1 || docker network create app-net
          docker inspect "$EDGE_CONTAINER" >/dev/null

      - name: Deploy candidate release without dropping the web upstream
        working-directory: /opt/apps/www-webigram.ir
        run: |
          set -euo pipefail
          ROLLBACK_DIR="$APP_DIR/.deploy-rollback"
          PREVIOUS_SHA=""
          [ -f "$ROLLBACK_DIR/main.sha" ] && PREVIOUS_SHA=$(cat "$ROLLBACK_DIR/main.sha")

          # Let Compose recreate only services whose Compose definition actually changed.
          docker compose up -d --remove-orphans

          # Server code is bind-mounted. Restarting keeps the API container/IP stable.
          docker compose restart website-doctor-api

          api_healthy=0
          for i in $(seq 1 30); do
            API_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-website-doctor-api 2>/dev/null || true)
            echo "api_restart_attempt=$i api=$API_STATUS"
            if [ "$API_STATUS" = "healthy" ]; then
              api_healthy=1
              break
            fi
            sleep 2
          done
          [ "$api_healthy" -eq 1 ]

          # Directus custom migrations only run during bootstrap. Because the migrations
          # directory is bind-mounted, adding a migration does not make Compose recreate
          # the container by itself. Restart Directus whenever CMS migrations/config changed.
          DIRECTUS_CHANGED=0
          if [ -z "$PREVIOUS_SHA" ]; then
            DIRECTUS_CHANGED=1
          elif ! git diff --quiet "$PREVIOUS_SHA" HEAD -- directus/migrations compose.yaml; then
            DIRECTUS_CHANGED=1
          fi

          if [ "$DIRECTUS_CHANGED" -eq 1 ]; then
            echo "Directus migration/config changed; restarting Directus so bootstrap applies migrations."
            docker compose restart directus

            directus_healthy=0
            for i in $(seq 1 45); do
              DIRECTUS_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-directus 2>/dev/null || true)
              echo "directus_restart_attempt=$i directus=$DIRECTUS_STATUS"
              if [ "$DIRECTUS_STATUS" = "healthy" ]; then
                directus_healthy=1
                break
              fi
              sleep 2
            done
            [ "$directus_healthy" -eq 1 ]
          else
            echo "No Directus migration/config changes detected; keeping CMS container up."
          fi

          # A bind-mounted single file can keep the old inode after git checkout.
          # Recreate web nginx only when its own nginx config changed.
          if [ -n "$PREVIOUS_SHA" ] && ! git diff --quiet "$PREVIOUS_SHA" HEAD -- docker/nginx.conf; then
            echo "Web nginx config changed; recreating only webigram."
            docker compose up -d --no-deps --force-recreate webigram
          fi

          web_ready=0
          for i in $(seq 1 30); do
            WEB_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-web 2>/dev/null || true)
            echo "web_ready_attempt=$i web=$WEB_STATUS"
            if [ "$WEB_STATUS" = "healthy" ]; then
              web_ready=1
              break
            fi
            sleep 2
          done
          [ "$web_ready" -eq 1 ]

          # Reload both nginx layers. The outer edge nginx resolves Docker hostnames
          # at config load time, so this refreshes the webigram-web IP after any recreate.
          docker exec webigram-web nginx -t
          docker exec webigram-web nginx -s reload
          docker exec "$EDGE_CONTAINER" nginx -t
          docker exec "$EDGE_CONTAINER" nginx -s reload

      - name: Verify containers and public site
        run: |
          set -euo pipefail
          healthy=0

          for i in $(seq 1 30); do
            WEB_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-web 2>/dev/null || true)
            API_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-website-doctor-api 2>/dev/null || true)
            DIRECTUS_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-directus 2>/dev/null || true)
            PUBLIC_CODE=$(curl -sS -o /dev/null -w '%{http_code}' --connect-timeout 3 --max-time 5 "${PUBLIC_HEALTH_URL}?deploy=${GITHUB_SHA}&attempt=${i}" || true)

            echo "attempt=$i web=$WEB_STATUS api=$API_STATUS directus=$DIRECTUS_STATUS public_http=$PUBLIC_CODE"

            if [ "$WEB_STATUS" = "healthy" ] && [ "$API_STATUS" = "healthy" ] && [ "$DIRECTUS_STATUS" = "healthy" ] && [ "$PUBLIC_CODE" = "200" ]; then
              healthy=1
              break
            fi
            sleep 2
          done

          if [ "$healthy" -ne 1 ]; then
            echo "Candidate release failed production health checks."
            docker compose -f "$APP_DIR/compose.yaml" ps || true
            docker logs --tail 200 webigram-web || true
            docker logs --tail 200 webigram-website-doctor-api || true
            docker logs --tail 200 webigram-directus || true
            docker logs --tail 200 "$EDGE_CONTAINER" || true
            exit 1
          fi

          curl -fsS --connect-timeout 5 --max-time 15 --retry 2 --retry-delay 2 \
            "https://webigram.ir/test6/?deploy=${GITHUB_SHA}" \
            -o "$RUNNER_TEMP/webigram-test6.html"
          grep -q 'id="orbital-canvas"' "$RUNNER_TEMP/webigram-test6.html"
          grep -q 'id="experience-controls"' "$RUNNER_TEMP/webigram-test6.html"
          echo "Public /test6 route contains the 3D experience."

          curl -fsS --connect-timeout 5 --max-time 15 --retry 2 --retry-delay 2 \
            "https://webigram.ir/home6/?deploy=${GITHUB_SHA}" \
            -o "$RUNNER_TEMP/webigram-home6.html"
          grep -q 'id="home6-canvas"' "$RUNNER_TEMP/webigram-home6.html"
          grep -q 'id="home6-tools"' "$RUNNER_TEMP/webigram-home6.html"
          echo "Public /home6 route contains the animated homepage."
          echo "Production release is healthy through the public endpoint."

      - name: Roll back automatically on deployment failure
        if: ${{ failure() }}
        run: |
          set -uo pipefail
          ROLLBACK_DIR="$APP_DIR/.deploy-rollback"

          echo "Deployment failed. Starting automatic rollback..."

          if [ ! -f "$ROLLBACK_DIR/available" ] || [ ! -f "$ROLLBACK_DIR/main.sha" ] || [ ! -f "$ROLLBACK_DIR/dist/index.html" ]; then
            echo "ERROR: No valid previous production snapshot is available to restore."
            exit 1
          fi

          PREVIOUS_SHA=$(cat "$ROLLBACK_DIR/main.sha")
          echo "Restoring previous application revision: $PREVIOUS_SHA"

          git -C "$APP_DIR" checkout -B main "$PREVIOUS_SHA" || exit 1

          # Restore static files in-place so the bind mount stays valid.
          mkdir -p "$APP_DIR/dist"
          cp -a "$ROLLBACK_DIR/dist/." "$APP_DIR/dist/" || exit 1

          cd "$APP_DIR" || exit 1
          docker compose up -d --remove-orphans || exit 1
          docker compose restart website-doctor-api || exit 1

          api_healthy=0
          for i in $(seq 1 30); do
            API_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-website-doctor-api 2>/dev/null || true)
            [ "$API_STATUS" = "healthy" ] && api_healthy=1 && break
            sleep 2
          done
          [ "$api_healthy" -eq 1 ] || exit 1

          # Force web recreation during rollback so any reverted bind-mounted nginx file
          # is guaranteed to be the one nginx sees.
          docker compose up -d --no-deps --force-recreate webigram || exit 1

          web_ready=0
          for i in $(seq 1 30); do
            WEB_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-web 2>/dev/null || true)
            [ "$WEB_STATUS" = "healthy" ] && web_ready=1 && break
            sleep 2
          done
          [ "$web_ready" -eq 1 ] || exit 1

          docker exec webigram-web nginx -t || exit 1
          docker exec webigram-web nginx -s reload || exit 1
          docker exec "$EDGE_CONTAINER" nginx -t || exit 1
          docker exec "$EDGE_CONTAINER" nginx -s reload || exit 1

          rollback_healthy=0
          for i in $(seq 1 30); do
            WEB_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-web 2>/dev/null || true)
            API_STATUS=$(docker inspect --format='{{.State.Health.Status}}' webigram-website-doctor-api 2>/dev/null || true)
            PUBLIC_CODE=$(curl -sS -o /dev/null -w '%{http_code}' --connect-timeout 3 --max-time 5 "${PUBLIC_HEALTH_URL}?rollback=${PREVIOUS_SHA}&attempt=${i}" || true)

            echo "rollback_attempt=$i web=$WEB_STATUS api=$API_STATUS public_http=$PUBLIC_CODE"

            if [ "$WEB_STATUS" = "healthy" ] && [ "$API_STATUS" = "healthy" ] && [ "$PUBLIC_CODE" = "200" ]; then
              rollback_healthy=1
              break
            fi
            sleep 2
          done

          if [ "$rollback_healthy" -ne 1 ]; then
            echo "ERROR: Rollback completed but the restored release is still unhealthy."
            docker compose ps || true
            docker logs --tail 200 webigram-web || true
            docker logs --tail 200 webigram-website-doctor-api || true
            docker logs --tail 200 "$EDGE_CONTAINER" || true
            exit 1
          fi

          echo "Rollback successful. Previous production release is healthy again."

      - name: Record successful deployment
        if: ${{ success() }}
        run: |
          set -euo pipefail
          git -C "$APP_DIR" rev-parse HEAD > "$APP_DIR/.last-successful-deploy"
          echo "Successful production revision: $(cat "$APP_DIR/.last-successful-deploy")"
